# Frontend API URL for PHP endpoints
VITE_BACKEND_URL=https://tap4service.co.nz/api

# WebSocket URL (commented out as MyHost doesn't support WebSocket servers)
# VITE_WEBSOCKET_URL=wss://tap4service.co.nz:5001

# Database variables (optional, only needed for a Node.js backend)
# DB_HOST=localhost (use MyHost MySQL hostname for remote access, e.g., mysql.tap4service.co.nz)
# DB_USER=tapservi_deploy
# DB_PASSWORD=WAKO123database
# DB_NAME=tapservi_tap4service
# DB_PORT=3306 (default MySQL port, optional)